/**
 * Point d'entrée du package backend.
 *
 * Une fois `bunx convex dev` lancé depuis `packages/backend`, Convex génère
 * `convex/_generated/api.ts` qui expose les queries/mutations typées.
 * Les apps les importent via `@workspace/backend/generated`.
 */

export const BACKEND_PACKAGE_VERSION = "0.0.0"
